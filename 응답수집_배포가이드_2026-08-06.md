# 설문 응답 수집·조회 배포 가이드

- 작성일: 2026-08-06
- 구성: 공개 HTML 설문 페이지 → (POST) → Apps Script 웹앱 → **기획자 소유 구글 시트**
- 서버 없음 · API 키 없음 · 비용 없음

## 소유권 모델 (먼저 확인)

응답은 **설문을 만든 사람 계정의 시트 한 곳**에만 쌓인다. 응답자 쪽에는 아무것도 생기지 않는다.

| | 응답자 | 기획자(소유자) |
|---|---|---|
| 구글 로그인 | 불필요 | 시트 볼 때만 |
| 시트 접근 | **불가능** (읽기 경로 자체가 없음) | 전체 조회·편집 |
| 브라우저에 노출되는 값 | 웹앱 URL 하나 (쓰기 전용) | — |

이걸 보장하는 것이 4단계의 **`실행: 나`** 설정이다. 이 설정이면 스크립트가 소유자 권한으로 돌아가므로
누가 제출하든 항상 소유자의 시트에 기록된다. `실행: 웹앱에 액세스하는 사용자`로 배포하면
응답자에게 구글 로그인을 요구하게 되므로 **절대 그렇게 배포하지 말 것.**

웹앱 URL은 자격증명이 아니다. 이 주소로는 기존 응답의 조회·수정·삭제가 불가능하고(append-only),
가능한 최악은 스팸 행 유입이며 이는 허니팟·Turnstile·유량제한으로 막고 시트에서 행을 지우면 끝난다.

---

## 1. 시트 만들기

1. [sheets.new](https://sheets.new) 로 새 스프레드시트 생성 → 이름: `AI도입의향조사_응답`

> 이 파일은 기획자 계정의 내 드라이브에 있고, 공유하지 않는 한 아무도 볼 수 없다.

## 2. Apps Script에 코드 붙여넣기

1. **그 시트의** 상단 메뉴 **확장 프로그램 → Apps Script**
   (반드시 시트에서 열 것. 이렇게 만든 '바인딩 스크립트'는 자신이 속한 시트를 자동으로 찾으므로
   시트 ID를 어디에도 적을 필요가 없다.)
2. 기본으로 열린 `Code.gs` 내용을 **전체 선택 후 삭제**하고, `응답수집_AppsScript_코드.gs` 전체를 붙여넣기
3. 저장(⌘S). 프로젝트 이름은 `설문응답수집` 정도로.

> 코드는 **고칠 곳이 없다.** `SHEET_ID`는 빈 값으로 두면 된다.
> (독립형 스크립트로 만든 경우에만 그 값에 시트 ID를 넣는다.)

## 3. 동작 확인 (배포 전)

상단 함수 선택 목록에서 **`설치테스트`** 를 고르고 **실행**.

- 최초 1회 권한 승인 화면이 뜬다: **권한 검토 → 계정 선택 → "이 앱은 확인되지 않았습니다" → 고급 → (프로젝트 이름)(으)로 이동 → 허용**
  - 본인이 만든 스크립트라 정상이다. 이 경고는 구글 심사를 받지 않은 개인 스크립트에 항상 뜬다.
- 시트로 돌아가 `응답` 탭에 헤더행과 테스트 행 1건이 생겼는지 확인 → **테스트 행은 삭제**

여기까지 되면 시트 연결은 끝난 것이다.

## 4. 웹앱으로 배포 ★가장 중요★

우측 상단 **배포 → 새 배포 → 유형(톱니바퀴) → 웹 앱**

| 항목 | 설정값 |
|---|---|
| 설명 | `v1` |
| **실행** | **나 (본인 계정)** ← 이걸 틀리면 응답자에게 로그인을 요구하게 됨 |
| **액세스 권한이 있는 사용자** | **모든 사용자** ← 로그인 없이 제출 가능해야 하므로 |

배포 후 나오는 **웹 앱 URL**(`https://script.google.com/macros/s/AKfy.../exec`)을 복사한다.

> 브라우저에서 이 URL을 그냥 열어 `{"ok":true,"service":"orca-survey-collector"}` 가 보이면 정상이다.
> (이 GET 응답은 상태 확인용일 뿐 응답 데이터를 일절 반환하지 않는다.)

**⚠️ 코드를 수정한 뒤에는 반드시 `배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포`** 를 해야 반영된다.
저장만 하면 기존 URL은 옛 코드를 계속 실행한다. 가장 흔한 함정이다.

## 5. HTML 페이지에 URL 연결

`오르카아이티_AI도입의향_설문_웹페이지.html` 의 `<script>` 상단:

```js
const ENDPOINT = "https://script.google.com/macros/s/AKfy.../exec";
```

이제 페이지를 열어 설문을 끝까지 채우고 제출하면 → 감사 화면이 뜨고 → 시트에 행이 추가된다.

- `ENDPOINT`가 비어 있거나 전송이 실패하면 기존의 **수동 접수 화면**(메일·복사·파일 저장)으로 자동 폴백한다.
  응답자가 입력한 내용은 그 화면에 그대로 보존되므로 어떤 경우에도 응답이 증발하지 않는다.

## 6. 페이지 호스팅

HTML 파일 하나뿐이라 아무 정적 호스팅이나 가능하다.

- **Netlify Drop** ([app.netlify.com/drop](https://app.netlify.com/drop)) — 파일을 끌어다 놓으면 즉시 URL 발급. 가장 빠름
- **GitHub Pages** — 회사 계정으로 이력 관리까지 원할 때
- 사내 웹서버 / 기존 회사 홈페이지 하위 경로

> 로컬 `file://` 로 열면 브라우저가 요청을 차단할 수 있으므로, 테스트도 호스팅한 URL에서 하는 것이 정확하다.

## 7. 결과 조회

**1번에서 만든 시트를 열면 그것이 곧 관리자 화면이다.** 별도 개발이 필요 없다.

- 실시간 누적 · 본인 계정으로 이미 인증됨 · 외부 비공개
- 필터·정렬·피벗테이블로 집계
- **파일 → 다운로드 → Microsoft Excel(.xlsx)** 로 언제든 내보내기
- 팀원과 공유하려면 시트 우측 상단 **공유**로 계정을 지정해 추가 (링크 공개는 하지 말 것)
- 새 응답 알림을 받으려면 시트에서 **도구 → 알림 규칙 → 사용자가 양식을 제출할 때 → 즉시 이메일**

---

## 8. (선택) 스팸이 실제로 생기면 캡차 켜기

지금 상태로도 허니팟이 자동화 봇 대부분을 거른다. 그래도 스팸이 들어오면:

1. [Cloudflare Turnstile](https://dash.cloudflare.com/?to=/:account/turnstile) 에서 사이트 등록 → **사이트 키**와 **시크릿 키** 발급
2. HTML: `const TURNSTILE_SITE_KEY = "0x4AAA...";` (공개값이라 노출돼도 무해)
3. Apps Script: **프로젝트 설정(⚙) → 스크립트 속성 → 속성 추가**
   - 속성 `TURNSTILE_SECRET` / 값 `0x4AAA...시크릿`
   - **시크릿 키는 절대 HTML이나 코드에 넣지 않는다.** 스크립트 속성에만 둔다.
4. 재배포(4단계의 ⚠️ 참고)

속성을 설정하지 않으면 검증은 자동으로 생략되므로, 캡차 없이 먼저 운영을 시작해도 된다.

## 방어 요약

| 단계 | 수단 | 비고 |
|---|---|---|
| 1 | 허니팟 | 봇에게 차단 사실을 알리지 않고 조용히 폐기 |
| 2 | 전역 유량제한 (분당 30건) | Apps Script는 요청자 IP를 제공하지 않아 IP별 제한은 불가 |
| 3 | Turnstile 서버측 검증 | 선택 — 실질적 방어선 |
| 4 | 필드 화이트리스트 · 1000자 제한 | 정의된 열만 저장 |
| 5 | 수식 인젝션 차단 | `=`, `+`, `-`, `@` 로 시작하는 값은 텍스트로 고정 |

## 문항을 바꿀 때

HTML의 `collectAnswers()` 반환 객체와 `코드.gs`의 `COLUMNS` 배열은 **key가 1:1로 대응**한다.
문항을 추가·삭제하면 양쪽을 함께 고치고 재배포할 것. (현재 20개 항목, 순서 일치 확인 완료)
