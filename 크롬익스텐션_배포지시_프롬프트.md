# Claude for Chrome 배포 지시 프롬프트

## 사전 준비 (터미널에서 1회)

Apps Script 에디터는 자동완성·자동 들여쓰기가 있어서 에이전트가 코드를 **타이핑**하면 반드시 깨진다.
붙여넣기로만 처리해야 하므로, 먼저 코드를 클립보드에 올려둔다.

```bash
cd "/Users/daniel/Projects/company/2026-08_영림원오르카_AI도입의향_고객설문"
pbcopy < 응답수집_AppsScript_코드.gs
```

그리고 크롬에서 **구글 계정에 미리 로그인**해 둔다(응답을 받을 계정 = 설문 기획자 계정).
여러 계정에 로그인되어 있으면 의도치 않은 계정에 시트가 생길 수 있으니, 가급적 해당 계정만 로그인된
프로필/창에서 실행한다.

---

## 프롬프트 (아래 블록 전체를 복사해 Claude for Chrome에 붙여넣기)

```
구글 설문 응답 수집용 시트와 Apps Script 웹앱을 만들어 줘. 아래 순서를 그대로 따라줘.

[배경]
외부에 공개될 HTML 설문 페이지가 POST로 응답을 보내면, 그걸 내 구글 시트에 쌓는 엔드포인트를 만드는 작업이야.
붙여넣을 코드는 이미 내 클립보드에 들어 있어. 코드는 한 글자도 수정할 필요 없어.

[1단계 — 시트 생성]
1. https://sheets.new 로 이동해서 새 스프레드시트를 만들어.
2. 문서 이름을 "AI도입의향조사_응답" 으로 바꿔.
3. 이 시트는 절대 공개하거나 링크 공유로 전환하지 마. 비공개 상태 그대로 둬.

[2단계 — Apps Script 열기]
4. 그 시트의 상단 메뉴에서 [확장 프로그램] → [Apps Script] 를 눌러. 새 탭이 열릴 거야.
   ※ 반드시 이 경로로 열어야 해. 시트에 바인딩된 스크립트여야 코드가 시트를 자동으로 찾거든.
     script.google.com 에서 따로 새 프로젝트를 만들지 마.
5. 프로젝트 이름을 "설문응답수집" 으로 바꿔.

[3단계 — 코드 붙여넣기]
6. 코드 에디터 영역을 클릭한 다음, Cmd+A 로 전체 선택하고 Cmd+V 로 붙여넣어.
   ※ 코드를 직접 타이핑하지 마. 반드시 클립보드 붙여넣기로만 해. 타이핑하면 자동 들여쓰기 때문에 깨져.
7. Cmd+S 로 저장해.
8. 붙여넣기가 온전한지 확인해줘:
   - 첫 줄이 "/**" 로 시작하는 주석인지
   - 파일 안에 doPost, doGet, appendRow, verifyTurnstile, 설치테스트 함수가 모두 있는지
   - 에디터 왼쪽에 빨간 오류 표시가 없는지
   하나라도 이상하면 멈추고 나한테 알려줘.

[4단계 — 권한 승인 (여기서 나에게 넘겨줘)]
9. 상단 함수 선택 드롭다운에서 "설치테스트" 를 고르고 [실행] 을 눌러.
10. 권한 승인 창이 뜰 거야. 이건 내 계정 권한을 부여하는 민감한 단계니까,
    창이 뜨면 **거기서 멈추고 나한테 제어를 넘겨줘.** 내가 직접 승인할게.
    (참고: "이 앱은 확인되지 않았습니다" 경고는 내가 만든 개인 스크립트라 정상이야)
11. 내가 승인을 끝내고 알려주면 다시 이어서 진행해.

[5단계 — 적재 확인]
12. 실행이 성공하면 시트 탭으로 돌아가서 "응답" 이라는 새 시트 탭이 생겼는지 확인해.
13. 1행에 "접수시각 | 회사명 | 담당자 | 직책 ..." 헤더가 있고, 2행에 "테스트회사 / 홍길동" 테스트 행이
    들어와 있어야 해. 확인했으면 그 2행(테스트 행)을 삭제해줘. 헤더 1행은 남겨둬.

[6단계 — 웹앱 배포 ★가장 중요★]
14. Apps Script 탭으로 돌아가 우측 상단 [배포] → [새 배포] 를 눌러.
15. 좌측 [유형 선택] 톱니바퀴 → [웹 앱] 선택.
16. 설정을 정확히 이렇게 맞춰. 이 두 개를 틀리면 전체가 무용지물이 되니까 배포 누르기 전에 다시 한번 읽어줘:
    - 설명: v1
    - 다음 사용자로 실행(Execute as): **나** (내 계정)
    - 액세스 권한이 있는 사용자(Who has access): **모든 사용자** (Anyone)
    ※ "실행"을 "웹 앱에 액세스하는 사용자"로 두면 설문 응답자에게 구글 로그인을 요구하게 돼서 안 돼.
    ※ "액세스 권한"을 "나만"으로 두면 외부 응답자가 제출을 못 해서 안 돼.
17. [배포] 를 눌러. 추가 권한 승인 창이 뜨면 4단계처럼 나에게 넘겨줘.
18. 배포 완료 후 나오는 "웹 앱 URL" (https://script.google.com/macros/s/... 로 시작하고 /exec 로 끝남)을 복사해.

[7단계 — 동작 검증]
19. 새 탭에서 그 웹 앱 URL을 그대로 열어봐.
20. 화면에 {"ok":true,"service":"orca-survey-collector"} 이 나오면 성공이야.
    다른 게 나오거나 오류 페이지면 멈추고 화면 내용을 나한테 알려줘.

[마지막 — 결과 보고]
아래 3개를 텍스트로 정리해서 알려줘:
  1) 시트 URL (전체 주소)
  2) 웹 앱 URL (/exec 로 끝나는 주소)
  3) 6단계에서 실제로 설정한 "실행" / "액세스 권한" 값

[하지 말 것]
- 시트를 공개 공유하거나 링크 공유로 바꾸지 마.
- 코드를 임의로 수정하지 마. SHEET_ID 는 빈 값이 정상이야.
- 권한 승인 화면을 나 대신 클릭하지 마. 반드시 나에게 넘겨.
- 진행 중 예상과 다른 화면이 나오면 추측해서 진행하지 말고 멈추고 물어봐.
```

---

## 받은 결과를 반영하는 법

에이전트가 알려준 **웹 앱 URL**을
[web/index.html](web/index.html) 의 `ENDPOINT` 에 넣으면 끝난다.

```js
const ENDPOINT = "https://script.google.com/macros/s/AKfy.../exec";
```

이후 코드를 수정할 때는 **저장만으로는 반영되지 않는다.**
반드시 [배포] → [배포 관리] → 연필(편집) → 버전: **새 버전** → [배포] 를 거쳐야 한다.

---

# 이미 배포된 웹앱을 갱신할 때 (업데이트 재배포)

최초 배포와 절차가 다르다. **[새 배포]를 누르면 URL이 새로 발급되어** 페이지의 `ENDPOINT`가
옛 주소를 가리키게 되고, 응답이 예전 버전으로 들어가거나 끊긴다.
반드시 **기존 배포를 편집해서 버전만 올려야** URL이 유지된다.

## 사전 준비 (터미널에서 1회)

```bash
cd "/Users/daniel/Projects/company/2026-08_영림원오르카_AI도입의향_고객설문"
pbcopy < 응답수집_AppsScript_코드.gs
```

## 프롬프트 (아래 블록 전체를 복사해 Claude for Chrome에 붙여넣기)

```
내 구글 시트에 붙어 있는 Apps Script 웹앱의 코드를 갱신하고 재배포해줘.
새 코드는 이미 내 클립보드에 들어 있어. 한 글자도 수정할 필요 없어.

[1단계 — 스크립트 열기]
1. 구글 드라이브에서 "AI도입의향조사_응답" 스프레드시트를 열어.
2. [확장 프로그램] → [Apps Script] 를 눌러. "설문응답수집" 프로젝트가 열릴 거야.
   ※ script.google.com 에서 새 프로젝트를 만들지 마. 기존 프로젝트를 그대로 써야 해.

[2단계 — 코드 교체]
3. 코드 에디터를 클릭하고 Cmd+A 로 전체 선택한 뒤 Cmd+V 로 붙여넣어.
   ※ 절대 타이핑하지 마. 자동 들여쓰기 때문에 깨져. 붙여넣기로만.
4. Cmd+S 로 저장해.
5. 붙여넣기가 온전한지 확인해줘. 이번 갱신으로 새로 들어간 것들이야:
   - const DEDUPE_TTL_SEC = 600;   이 줄이 있는지
   - function dedupeKey(id) {      이 함수가 있는지
   - appendRow 안에 cache.get / cache.put 이 있는지
   - 에디터 왼쪽에 빨간 오류 표시가 없는지
   하나라도 없으면 멈추고 나한테 알려줘.

[3단계 — 재배포 ★여기가 핵심★]
6. 우측 상단 [배포] → [배포 관리] 를 눌러. (※ [새 배포] 아니야. 누르지 마.)
7. 목록에 있는 기존 배포(활성 상태)의 우측 **연필(수정) 아이콘**을 눌러.
8. [버전] 드롭다운을 열어 **"새 버전"** 을 선택해.
9. 설명란에 "v2 중복차단" 이라고 적어.
10. "다음 사용자로 실행"이 **나**, "액세스 권한이 있는 사용자"가 **모든 사용자** 인지
    다시 확인해. 이 두 값은 건드리지 말고 그대로여야 해.
11. [배포] 를 눌러. 권한 승인 창이 뜨면 거기서 멈추고 나에게 제어를 넘겨줘. 내가 승인할게.

[4단계 — URL이 그대로인지 확인 ★반드시★]
12. 배포 후 표시되는 웹 앱 URL을 나한테 알려줘.
13. 그 URL이 아래와 정확히 같아야 해:
    https://script.google.com/macros/s/AKfycbyiJFGQOx1980HDH0mA-wp46syQsmWd5Z1ZzDxs9a_b0JLrmtavkdu57WZ5tgDxEhNx/exec
    다르면 [새 배포]를 눌러버린 거야. 그 경우 멈추고 나한테 알려줘.

[5단계 — 동작 확인]
14. 새 탭에서 그 웹 앱 URL을 그대로 열어봐.
15. {"ok":true,"service":"orca-survey-collector"} 가 나오면 성공이야.

[하지 말 것]
- [새 배포] 를 누르지 마. 반드시 [배포 관리] → 연필 → 새 버전 경로로만.
- "실행" / "액세스 권한" 설정을 바꾸지 마.
- 코드를 임의로 수정하지 마.
- 권한 승인 화면을 나 대신 클릭하지 마.
```
