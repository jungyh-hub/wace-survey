# 안전망 — Vercel의 Root Directory를 `web` 으로 지정하면 애초에 web/ 밖은 배포되지 않는다.
# 이 파일은 그 설정을 빠뜨렸을 때 내부 문서가 공개 URL로 노출되는 것을 막기 위한 이중 방어다.
#
# 배포되어야 하는 것은 web/ 안의 설문 페이지뿐이며, 아래는 전부 내부 문서다.

*.pdf
*.docx
*.py
*.md
*.gs
_archive/
